# Local .terraform directory (contains provider binaries)
.terraform/

# Terraform state files
terraform.tfstate
terraform.tfstate.backup

# Terraform plan files (optional)
*.tfplan

# Lock file (optional but recommended to ignore)
.terraform.lock.hcl

# Crash logs
crash.log

# Ignore sensitive variable files if any
*.tfvars
